<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/>
    </parent>

    <groupId>com.proxyviewer</groupId>
    <artifactId>proxy-subscription-viewer</artifactId>
    <version>1.0.0</version>
    <name>Proxy Subscription Viewer</name>
    <description>显示代理订阅节点信息的Web应用</description>

    <properties>
        <java.version>21</java.version>
    </properties>

    <dependencies>
        <!-- Spring MVC Web（Spring Boot 4 起 spring-boot-starter-web 已废弃，改用 starter-webmvc） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>

        <!-- Thymeleaf 模板引擎 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>

        <!--
          Spring Security：登录认证（表单登录 + 会话）与 CSRF Token 防护。
          取代原先自研的 BasicAuthFilter（HTTP Basic），配置键仍是 app.security.*。
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!--
          Jackson：Spring Boot 4 起默认使用 Jackson 3，坐标由
          com.fasterxml.jackson.core 迁移到 tools.jackson.core，
          代码里的包名相应改为 tools.jackson.databind.*。
        -->
        <dependency>
            <groupId>tools.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <!-- Spring Data JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- MySQL 驱动 -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- 单元测试（JUnit 6 + AssertJ + Mockito + MockMvc） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!--
          Spring Boot 4 起 Web MVC 的测试自动配置（@AutoConfigureMockMvc 等）
          从 spring-boot-test-autoconfigure 拆到了独立模块 spring-boot-webmvc-test，
          不再随 spring-boot-starter-test 传递进来，需要显式引入。
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Spring Security 测试支持（MockMvc 的 csrf()/user()/formLogin() 等请求后处理器） -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- 测试用内存数据库（仅在测试期替代 MySQL），版本由 Spring Boot 统一管理 -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <jvmArguments>--enable-native-access=ALL-UNNAMED</jvmArguments>
                </configuration>
            </plugin>
            <!--
              Mockito 的 mock maker：项目使用 subclass mock maker
              （见 src/test/resources/mockito-extensions/），不依赖运行时挂载 Java agent。
              JDK 21+ 默认禁止动态挂载 agent（JEP 451），subclass 方式可以规避该限制。
              Spring Boot 4.1.1 管理的 Mockito 5.23.0 / Byte Buddy 1.18.11 已原生支持 JDK 25，
              因此不再需要手工提升这两个依赖的版本。
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- 测试里的诊断输出直接打印，便于排查网络/内核问题 -->
                    <redirectTestOutputToFile>false</redirectTestOutputToFile>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
